import React from 'react';

const recherche = () => {
    return (
        <div class="recherche">
            <input type="text" placeholder="Rechercher" />
        </div>
    );
};

export default recherche;